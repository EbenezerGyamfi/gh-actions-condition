name: delve-into-job
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    name: First Job
    steps:
      - name: Run First
        run: echo "Running first"
    
  second-job:
    runs-on: ubuntu-
    name: Second Job
    needs: first-job
    steps:
      - name: Run Second
        run: echo "Running second"
